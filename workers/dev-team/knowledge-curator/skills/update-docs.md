# update-docs

Update knowledge base and docs after a change.

## Process
1. Identify what changed (agent, bank, CI, UI)
2. Update relevant files in `knowledge/rbt-trail/` or `docs/`
3. Check `docs/TEAM.md` worker routing still accurate
4. Run knowledge-curator drift check
