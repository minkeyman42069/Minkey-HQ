# tune-draft

Skill for `boon-architect` worker. Arguments: `$ARGUMENTS`

1. Read `src/agents/boon-architect.js`
2. Implement the requested change
3. `npm run build:trail && npm run verify:trail`
4. Report what changed
