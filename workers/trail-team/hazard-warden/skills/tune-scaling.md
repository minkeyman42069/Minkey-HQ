# tune-scaling

Skill for `hazard-warden` worker. Arguments: `$ARGUMENTS`

1. Read `src/agents/hazard-warden.js`
2. Implement the requested change
3. `npm run build:trail && npm run verify:trail`
4. Report what changed
